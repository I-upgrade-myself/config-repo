server:
  port: 8761

eureka:
  instance:
    hostname: discovery-service   # ім’я контейнера
  client:
    register-with-eureka: false   # сервер сам не реєструється
    fetch-registry: false         # не намагається отримувати реєстр
    # service-url:
      # defaultZone: http://discovery-service:8761/eureka/ 